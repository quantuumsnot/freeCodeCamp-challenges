function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  // This word play should stop, because in fact nobody cares about Ghost ...
  // ... hell, nobody cares for the planet. And this is the Problem!
  
  return bool === true || bool === false;
}

booWho(null);
